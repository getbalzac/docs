---
title: Installation
---

Install Balzac using Cargo:

```bash
cargo install balzac
```

## Requirements

- Rust toolchain (latest stable recommended)
- No additional dependencies required

## Verify Installation

After installation, verify Balzac is installed:

```bash
balzac --help
```

You should see the available commands, typically starting with `balzac build`.
